@model int

@if (Model > 0)
{
    <li>
        <a asp-controller="ShoppingCart" asp-action="Items">
            <span class="glyphicon glyphicon-shopping-cart"></span>
            <span id="cart-status">
                @Model
            </span>
        </a>
    </li>
}


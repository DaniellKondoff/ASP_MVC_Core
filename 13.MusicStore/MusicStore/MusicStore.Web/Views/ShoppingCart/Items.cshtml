@model IEnumerable<SongShoppingDetailsServiceModel>

<h2>Your shopping cart</h2>
<h4>Here are the delicious pies in your shopping cart.</h4>
<table class="table table-bordered table-striped">
    <thead>
        <tr>
            <th>Selected amount</th>
            <th>Song</th>
            <th class="text-right">Price</th>
            <th class="text-right">Subtotal</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td class="text-center">@item.Quantity</td>
                <td class="text-left">@item.Name</td>
                <td class="text-right">@item.Price.ToString("c")</td>
                <td class="text-right">
                    @((item.Quantity * item.Price).ToString("c"))
                </td>
                <td>
                    <a class="btn btn-primary" asp-controller="ShoppingCart" asp-action="RemoveFromCart" asp-route-id="@item.Id">Remove Item</a>
                </td>
            </tr>
        }
    </tbody>
    <tfoot>
        <tr>
            <td colspan="3" class="text-right">Total:</td>
            <td class="text-right">
                @Model.Sum(s => s.Price * s.Quantity).ToString("c")
            </td>
            @if (Model.Count() > 0)
            {
                <td>
                    <a class="btn btn-primary" asp-controller="ShoppingCart" asp-action="FinishOrder">Finish Order</a>
                </td>
            }
        </tr>
    </tfoot>
</table>